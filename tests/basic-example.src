sum(a: int, b: int): int
  return a + b

sum(values: List<Int>): int
  return values

plus_one(a: int): int
  let scoped = 0
  return a + 1

if(1 + 2 * 3)
  if(true)
    let scoped = 0f
    let s = scoped
    let a = "hello mama"
    let c = a
    let c = false
    let k = c
else
  let a = 1
let a = false